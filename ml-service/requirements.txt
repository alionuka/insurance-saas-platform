fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# ML stack — versions pinned to match the runtime used when the .joblib
# model files were serialized. Bumping these breaks unpickling
# (see ModuleNotFoundError: '_loss' on sklearn 1.9 with models from 1.8).
scikit-learn==1.8.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
joblib>=1.3.0,<2.0.0

# Visualization (training only — required by training/train_*.py scripts)
matplotlib>=3.7.0

# SHAP explanations
shap>=0.42.0,<1.0.0
