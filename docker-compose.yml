
services:
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"

  # backend:
  #   build: ./backend
  #   ports:
  #     - "3001:3001"

  # ml-service:
  #   build: ./ml-service
  #   ports:
  #     - "8000:8000"

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: insurance_saas_db
    ports:
      - "5433:5432"
